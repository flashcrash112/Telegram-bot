.env
.env.secrets
seen_tokens.json
positions.json
buy_history.json

# Telegram session stores: GramJS StoreSession creates a folder per name
# in TELEGRAM_SESSIONS, and those folders contain live account
# credentials. Add a line here for every session name you configure.
/sniper/
/alt/
*.session
*.session-journal
*authKey*

# Node
node_modules/
dist/

# Old Python leftovers
__pycache__/
*.pyc
.venv/
venv/
